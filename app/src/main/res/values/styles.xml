<resources>

    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="android:Theme.Holo">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle</item>
        <item name="android:imageButtonStyle">@style/Theme.Calculator.ButtonStyle.ImageButton</item>
    </style>

    <!--Light Theme -->
    <style name="Theme.Calculator.Light" parent="android:Theme.Holo">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle.Light</item>
        <item name="android:imageButtonStyle">
            @style/Theme.Calculator.ButtonStyle.ImageButton.Light
        </item>
    </style>

    <!-- Crimson Red Theme -->
    <style name="Theme.Calculator.Cred" parent="Theme.Calculator.Light">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle.Cred</item>
    </style>

    <!-- Celadon Green Theme -->
    <style name="Theme.Calculator.Cgreen" parent="Theme.Calculator.Light">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle.Cgreen</item>
    </style>

    <!-- Cerise Pink Theme -->
    <style name="Theme.Calculator.Cpink" parent="Theme.Calculator.Light">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle.Cpink</item>
    </style>

    <!-- Palatinate Purple Theme -->
    <style name="Theme.Calculator.Ppurple" parent="Theme.Calculator.Light">
        <item name="android:buttonStyle">@style/Theme.Calculator.ButtonStyle.Ppurple</item>
    </style>

    <!-- Button Style -->
    <style name="Theme.Calculator.ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:textSize">@dimen/btn_text_size</item>
        <item name="android:textColor">@drawable/default_dark_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/default_dark_btn_default_holo_light</item>
    </style>

    <!--Light theme for Buttons -->
    <style name="Theme.Calculator.ButtonStyle.Light" parent="Theme.Calculator.ButtonStyle">
        <item name="android:background">@drawable/default_light_btn_default_holo_light</item>
        <item name="android:textColor">@drawable/default_light_text</item>
    </style>

    <!-- Crimson red theme for Buttons -->
    <style name="Theme.Calculator.ButtonStyle.Cred" parent="Theme.Calculator.ButtonStyle.Light">
        <item name="android:textColor">@drawable/crimson_red_text</item>
    </style>

    <!-- Celadon green theme for Buttons -->
    <style name="Theme.Calculator.ButtonStyle.Cgreen" parent="Theme.Calculator.ButtonStyle.Light">
        <item name="android:textColor">@drawable/celadon_green_text</item>
    </style>

    <!-- Cerise Pink theme for Buttons -->
    <style name="Theme.Calculator.ButtonStyle.Cpink" parent="Theme.Calculator.ButtonStyle.Light">
        <item name="android:textColor">@drawable/cerise_pink_text</item>
    </style>

    <!-- Palatinate Purple theme for Buttons -->
    <style name="Theme.Calculator.ButtonStyle.Ppurple" parent="Theme.Calculator.ButtonStyle.Light">
        <item name="android:textColor">@drawable/palatinate_purple_text</item>
    </style>

    <!--Delete and Clear button Style-->
    <style name="Theme.Calculator.ButtonStyle.ImageButton" parent="android:Widget.ImageButton">
        <item name="android:background">@drawable/default_dark_op_btn_default_holo_light</item>
    </style>

    <!--Delete and Clear button Light theme Style-->
    <style name="Theme.Calculator.ButtonStyle.ImageButton.Light" parent="android:Widget.ImageButton">
        <item name="android:background">@drawable/default_light_op_btn_default_holo_light</item>
    </style>

    <!-- Display Style -->
    <style name="Theme.Calculator.Display">
        <item name="android:gravity">center_vertical|right</item>
        <item name="android:layout_gravity">right</item>
        <item name="android:textSize">@dimen/font_size</item>
        <item name="android:textColor">@color/dark_theme_text_color</item>
        <item name="android:maxLines">1</item>
        <item name="android:scrollbars">none</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:padding">@dimen/entryText_padding</item>
        <item name="android:maxHeight">@dimen/display_height</item>
    </style>
</resources>
